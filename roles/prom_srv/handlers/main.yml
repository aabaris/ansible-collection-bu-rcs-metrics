- name: Restart Prometheus
  ansible.builtin.systemd:
    name: "{{ prom_srv_name }}"
    state: restarted
    daemon_reload: true
  when: not ansible_check_mode

- name: Reload Prometheus
  ansible.builtin.systemd:
    name: "{{ prom_srv_name }}"
    state: reloaded
  when: not ansible_check_mode
