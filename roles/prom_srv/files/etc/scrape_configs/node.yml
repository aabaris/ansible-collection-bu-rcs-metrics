---
scrape_configs:
  - job_name: node
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/nodes.json
    relabel_configs:
      - source_labels: [__address__]
        regex: '([^.:]+).*'
        replacement: '$1'
        target_label: host
